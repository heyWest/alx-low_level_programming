/**
 * main - infinite loop
 *
 * Return: Always 0.
 */
int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n");

	i = 0;

	/*
	* while (i < 10)
	* {
	* Infinite loop - no variable increment
	*	putchar(i);
	* }
	*/
	printf("Infinite loop avoided! \\o/\n");

	return (0);
}
