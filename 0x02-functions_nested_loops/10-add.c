#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: takes a value to be computed
 * @b: takes a value to be computed
 * Return: Always 0.
 */
int add(int a, int b)
{
	return (a + b);
}
